{{yield (hash
  acts=(hash
    captureScrollElement=(fn this.captureScrollElement)
    submitMessage=(fn this.chatMessageSubmit)
    deleteMessage=(fn this.deleteChatMessage)
    updateMessages=(fn this.updateMessages)
    noFollowLink=(fn this.noFollowLink)
  )
  messages=this.messages
)}}